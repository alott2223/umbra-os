# Umbra OS — systemd manager hardening.
[Manager]
# Belt-and-suspenders with limits.d / coredump.conf: no core dumps.
DefaultLimitCORE=0:0
# Wipe credentials/secrets from the environment of spawned services promptly.
DefaultTimeoutStopSec=20s
# Constrain the default device policy for services that don't declare one.
