# Umbra OS — autologin the live user straight into the Xfce desktop.
# (i3 is still available: pick it from the greeter's session menu after logout.)
[Seat:*]
autologin-user=user
autologin-user-timeout=0
autologin-session=xfce
user-session=xfce
greeter-session=lightdm-gtk-greeter
allow-guest=false
